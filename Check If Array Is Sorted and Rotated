class Solution {
    public boolean check(int[] nums) {
        int n  = nums.length;
        int breakcount = 0;
        for(int i  =0; i< n; i++){
            if(nums[i%n]>nums[(i+1)%n]) breakcount++;
        }
        if(breakcount>1) return false;
        return true;
    }
}
