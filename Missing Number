class Solution {
    public int missingNumber(int[] nums) {
        int n = nums.length, total=0, curr=0;
        for(int i =0 ; i< n; i++){
            total+=(i+1);
            curr+=nums[i];
        }
        return total-curr;    
    }
}
